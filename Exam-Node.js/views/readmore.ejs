<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <title>Blue Ocean Card</title>
    <style>
        body {
            font-family: 'Karla', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #e0f7fa;
            color: #232323;
        }

        .container {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .card {
            background-color: #fff;
            width: 100%;
            max-width: 900px;
            box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.1);
            border-radius: 16px;
            display: grid;
            grid-template-columns: 1fr 1.5fr;
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.15);
        }

        .img-container {
            background-color: #c1b7b4;
            background-size: cover;
            background-position: center;
            height: 100%;
            border-radius: 16px 0 0 16px;
        }

        .img-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .card-content {
            padding: 30px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        h1 {
            font-size: 2.5em;
            color: #0277bd;
            margin: 0;
            font-weight: 600;
        }

        h2 {
            font-size: 1.1em;
            text-transform: uppercase;
            color: #0288d1;
            margin: 10px 0;
            font-weight: 400;
        }

        p.excerpt {
            font-size: 1.1em;
            color: #555;
            line-height: 1.8;
            margin-bottom: 20px;
        }

        .author {
            font-weight: 700;
            color: #0277bd;
            border-top: 2px solid #0288d1;
            padding-top: 20px;
            margin-top: 20px;
        }

        nav {
            z-index: 999;
            width: 300px;
            padding: 30px;
            background-color: white;
            box-shadow: 0 -3px 10px rgba(0, 0, 0, 0.2);
            display: flex;
            align-items: center;
            justify-content: space-evenly;
            rotate: 90deg;
            position: fixed;
            top: 50%;
            border-radius: 20px;
            margin-left: -80px;
        }

        a {
            rotate: -90deg;
            padding: 10px;
            text-decoration: none;
            color: rgb(24, 24, 24);
            transition: all .4s ease-in-out;
        }

        a:hover {
            transform: scale(1.2);
        }
    </style>
</head>

<body>

    <nav id="navbar">
        <a href="/dashboard"><i class="fas fa-house"></i></a>
        <a href="/addblog"><i class="fas fa-bag-shopping"></i></a>
        <a href="cart"><i class="fas fa-cart-shopping"></i></a>
    </nav>

    <div class="container">
        <div class="card">
            <div class="img-container">
                <img src="<%= readBlog.image %>" alt="City Street">
            </div>
            <div class="card-content">
                <h2>Hello</h2>
                <h1>
                    <%= readBlog.title %>
                </h1>
                <p class="excerpt">
                    <%= readBlog.description %>
                </p>
                <p class="author">By Jrom</p>
                
            </div>
        </div>
    </div>

</body>

</html>