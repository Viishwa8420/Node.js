<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.movie_name %> Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('https://assetscdn1.paytm.com/images/cinema/3-739c9360-86f7-11ef-b30d-5d8b2ded08e1%5B1%5D-d423dc50-9117-11ef-b907-a19bf42cde25.jpg'); 
            background-size: cover;
            background-position: center;
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {
            padding: 30px;
        }

        .card {
            margin: auto;
            height:300px;
            backdrop-filter: blur(10px);
            background-color: rgba(255, 245, 195, 0.7);
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

       
        .card h1 {
            font-size: 1.5rem;
            margin-bottom: 10px;
            font-weight: bold;
        }

        .card p {
            font-size: 1rem;
        }

        .card a {
            margin-top: 10px;
            width:180px;
        }

        @media (max-width: 768px) {
            .card {
                margin-bottom: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row">
                    
                    <div class="col-md-6">  
                        <img src="<%= movie.movie_image %>" class="img-fluid" alt="<%= movie.movie_name %>">
                    </div>
                    <div class="card col-md-6">
                        <h1 class="my-4"><%= movie.movie_name %></h1>
                        <p class="my-3"><strong>Description:</strong> <%= movie.description %></p>
                        <p><strong>Price:</strong> $<%= movie.price %></p>
                        <a href="/" class="btn btn-warning">Back to Movies</a>
                    </div>
               
            </div>

        </div>
</body>
</html>